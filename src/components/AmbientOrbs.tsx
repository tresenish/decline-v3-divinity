/**
 * Ambient background orbs - decorative floating elements.
 * Simplified version - renders nothing for now.
 */
import React from 'react';

type Props = {
  preset?: string;
};

const AmbientOrbs: React.FC<Props> = () => {
  // Decorative orbs removed for cleaner design
  return null;
};

export default AmbientOrbs;
